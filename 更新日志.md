# 更新日志

本文只记录公开版代码变更，不包含真实账号、服务器地址、登录态、Cookie、验证码、API 密钥或运行数据。

## 2026-09-02

### 系统设置保存交互修复（2026.09.02.1）

- 修复系统设置保存请求失败时页面没有反馈、看起来像按钮无响应的问题。
- 保存期间按钮显示加载状态并阻止重复提交；请求结束后自动恢复可操作状态。
- 服务端校验、SMTP 监听冲突或网络异常现在会在状态栏、运行日志和提示消息中显示具体原因。

## 2026-08-31

### 域名邮箱过期保护与历史地址恢复（2026.08.31.2）

- HTML 接码生命周期清理不再删除域名邮箱，避免 Cloudflare 或 SMTP 收件地址因 HTML 链接过期而返回 404。
- 手动“清理全部已过期”仅处理 iCloud 出库邮箱，域名邮箱继续保持可收件状态；域名邮箱仍可通过单行删除明确移除。
- 增加域名邮箱过期清理回归测试，覆盖自动清理和手动批量清理两条路径。

### 域名收件方式可选（2026.08.31.1）

- 接入域名时新增 Cloudflare 转发、自建 SMTP、Remail 三种收件方式选择，并将选择持久化到域名资产。
- 域名清单显示收件方式；DNS 指引根据所选方式分别给出 Cloudflare、SMTP 或 Remail 配置提示。
- Cloudflare 转发与自建 SMTP 按域名收件方式校验；历史域名保留兼容行为，不影响已有收件配置。

## 2026-08-29

### HTML 邮件按钮跳转恢复（2026.08.29.1）

- 修复 HTML 接码页清洗邮件内容时误删所有非锚点 `href` 的问题，邮件中的验证按钮和普通链接现在会恢复为可点击跳转。
- 仅允许 `http`、`https` 和 `mailto` 链接，移除脚本协议、事件属性及危险外部资源；外部链接在新标签页打开并保留沙箱隔离。
- 更新 HTML 预览提示与回归断言，验证按钮链接不会再次被格式化流程清空。

## 2026-08-24

### 二级域邮箱池与双随机地址格式（2026.08.24.7）

- 域名邮箱本地部分调整为 `随机6位-随机8位`，不再使用固定 `mail-` 前缀，生成结果符合 `随机-随机@二级邮件域` 格式。
- 单域生成与多域随机分配统一使用新的双随机地址生成器，并继续使用持久化历史记录保证地址永久去重。
- 为双随机地址格式和历史地址跳过逻辑补充回归测试，现有历史记录保持兼容。

### HTML 接码验证码语义标签修复（2026.08.24.6）

- HTML 接码页面的最新验证码容器由 `div.code` 改为语义化 `<code class="code" id="code">`，保留原有样式与动态刷新行为。
- 新增模板回归断言，确保后续 UI 修改不会丢失 `<code>` 标签。

### Apple 管理入口备用域名与删除降级修复（2026.08.24.5）

- 新接口管理 token、登录后的管理请求在 Apple 主入口返回 503/502 时自动尝试 `account.apple.com` 备用入口。
- 503/502/500/429 下的手动 iCloud 邮箱删除不再受 HTML 过期状态限制，本地记录立即清理并明确标记远端待重试。
- 测试环境的自定义 Apple 地址保持原有请求次数与断言，避免备用入口逻辑干扰回归测试。

### 过期邮箱降级清理日志修正（2026.08.24.4）

- 区分“远端待重试”的本地清理与真正完成远端删除，后台日志不再把两种结果混写为已完成。

### 过期邮箱删除降级与批量清理反馈修复（2026.08.24.3）

- Apple 管理接口持续返回 HTTP 503/502/500/429 时，过期邮箱不再卡在删除失败；本地邮箱、邮件和 HTML 地址会立即清理，并在结果中标记远端删除待重试。
- 手动删除过期邮箱与“清理全部过期邮箱”统一使用该降级路径，正常未过期邮箱仍保持远端删除成功后再清理本地的保护策略。
- 批量清理响应和界面新增“远端待重试”数量提示，避免把本地清理结果误报为完全远端删除。
- 新增 Apple 暂态故障下过期邮箱本地清理回归测试。

### 新接口登录反馈与 Apple 暂态恢复修复（2026.08.24.2）

- 修复新接口登录遇到 HTTP 503 时前端 Promise 未捕获、按钮看起来无响应的问题；现在会显示连接中、失败原因、HTTP 状态和可重试提示，并恢复按钮状态。
- Apple Account 管理 token 刷新增加 503/502/500/429 等暂态响应的有界重试。
- 删除过期邮箱时，即使 token 刷新服务短暂不可用，只要已保存管理 API Key 仍可用，流程会继续执行停用与删除；认证失败时再触发完整刷新。
- 新增登录反馈模板和 token 刷新暂态故障回归测试。

### 过期邮箱远端删除与批量清理稳定性修复（2026.08.24.1）

- 修复 Apple Account 删除请求出错时丢失 HTTP 状态码的问题；远端 404/410 现在会正确按“远端已不存在”处理并继续清理本地邮箱、邮件和 HTML 地址。
- 新接口停用后若 Apple 管理服务短暂返回 HTTP 409，删除流程会进行有界重试，避免因状态传播延迟导致过期邮箱一直清理失败。
- Apple 503/502/500/429 等暂态响应纳入重试；历史邮箱保存的新旧接口来源不一致时，会自动回退另一套可用登录态并按邮箱地址重新定位远端地址。
- 新增过期邮箱删除、接口回退、404 幂等和 409 状态传播回归测试。

## 2026-08-22

### iCloud 批量删除缓存稳定性修复（2026.08.22.4）

- 批量删除缓存键改用账号稳定标识，不再受 Apple Account 轮换 `scnt` 或管理 API Key 影响，确保同一账号连续清理时持续复用远端邮箱列表。

### iCloud 新接口远端删除与批量清理修复（2026.08.22.3）

- 新增 Apple Account 隐私邮箱列表、停用和永久删除链路，使用 `/account/manage/email/private`、`/{id}/stop` 与 `/{id}/remove`，修复新接口创建的邮箱被错误交给旧接口删除的问题。
- 删除时根据邮箱持久化的远端 ID 与来源选择新旧接口；历史数据缺少来源时会依次核对新旧远端，远端不存在按已删除处理，远端调用失败继续保留本地邮箱和邮件。
- 新建和远端导入邮箱会持久化远端 ID/来源；批量清理复用同一账号的新接口邮箱列表，避免每个邮箱重复请求完整列表。
- Apple Account 删除过程自动复核和刷新管理态，并保存轮换后的登录状态；旧接口停用与删除流程保持兼容。
- 新增新接口 HTTP 204、已保存远端 ID、批量列表缓存、元数据持久化及删除失败保留本地数据的回归测试。

### 修复登录态分层后的 iCloud 邮箱删除（2026.08.22.2）

- 修复旧接口 Cookie 保存在 `login_states[icloud_web]` 时，删除邮箱仍误报“缺少旧接口登录态”的问题。
- iCloud 邮箱删除、同步、远端邮件清理和旧接口创建统一使用有效的旧接口登录态，兼容新旧数据结构合并后的状态。
- 新增回归测试，覆盖仅保存 `icloud_web` 登录态 Cookie 时的停用与永久删除流程。

### Docker 多架构构建与服务器一键更新（2026.08.22.1）

- Docker 构建阶段改为使用构建机原生 Go 工具链交叉编译目标架构，避免 ARM 模拟编译长时间卡住。
- 保持 `compose.yaml` 默认拉取 `latest` 镜像，服务器执行 `docker compose up -d` 即可拉取并更新容器。
- 镜像继续发布 amd64/arm64 双架构，服务器不需要安装 Go 或执行本地源码构建命令。

## 2026-08-21

### 过期邮箱远端删除空响应兼容（2026.08.21.5）

- 修复 iCloud 隐私邮箱停用或永久删除接口返回 HTTP 204/空响应时被误判为删除失败的问题。
- 仅对无需解析返回体的远端变更请求接受成功空响应；需要结果数据的接口仍保留响应校验。
- 新增回归测试，覆盖活跃隐私邮箱在停用和删除阶段均返回 204 的完整清理流程。

### Cloudflare 多域名共用转发入口（2026.08.21.4）

- Cloudflare Worker 入站接口改为接受项目中所有已启用的接收域名，不再把转发配置中的主域名作为唯一允许域名。
- 每封邮件仍会校验 HMAC 签名、已接入域名、实际域名邮箱和邮箱启用状态；未接入域名及不存在的邮箱继续拒绝入库。
- 转发设置接口新增已启用域名列表，后台状态栏同时展示接收域名数量和域名邮箱数量；新增域名可直接复用现有 Worker、接收 API 与签名密钥。
- 增加主域名与第二域名共用转发入口的入站存储、验证码提取和重复邮件去重测试。

### 邮箱资产与出库工作台商业化升级（2026.08.21.3）

- “全部邮箱”和“取件码与发货”统一升级为商业化邮箱业务工作台，按库存运营、交付运营分别展示业务标题、说明和模式状态。
- 筛选、HTML 状态、业务操作和分页管理重新分层；操作区改为全宽自适应换行布局，修复按钮在宽度不足时越过面板边界的问题。
- 搜索框增加搜索图标和内嵌叉号清空按钮，仅在存在搜索内容时显示；点击叉号或按 Esc 均可清空当前页面独立搜索条件并立即刷新。
- 补充工作台结构、模式切换、清空交互和响应式布局测试，并在桌面、平板和手机视口完成无页面横向溢出的浏览器验收。

### 多域名随机生成与出库批次中心（2026.08.21.2）

- 域名邮箱新增“随机分配”开关；存在多个已启用域名时，可将批量生成任务逐地址随机投放到可用域名。
- 后端新增原子化多域名随机生成流程，统一执行域名权限、启用状态、永久历史去重和批量数量校验。
- 域名邮箱页面重构为商业化多域名资产工作台，接入与生成拆分为独立业务卡片，并以自绘主题化菜单替换原生域名下拉框。
- “取件码与发货”新增全部批次总览弹窗，展示当前所有出库批次、邮箱数量和最近更新时间；点击批次会自动回填搜索框并立即检索。
- 增加随机域名生成、随机 API、出库批次聚合、商业化页面结构与前端脚本语法测试，并完成本地浏览器交互验收。

### 域名邮箱永久历史去重（2026.08.21.1）

- 新增域名邮箱生成历史持久化记录，所有成功生成的地址会永久保留为去重依据。
- 生成时同时校验当前邮箱与历史地址；邮箱、用户或域名删除后，历史地址仍不会再次生成。
- 旧版本数据启动时自动将现有域名邮箱补录到历史记录，并统一规范大小写、合并重复项。
- 增加历史持久化、删除保留、碰撞重试、旧数据迁移和并发批量唯一性测试。

## 2026-08-20

### Cloudflare 转发邮箱收件网关（2026.08.20.2）

- 将侧边栏“转发邮箱”从 iCloud 登录态别名升级为独立的 Cloudflare Email Routing 工作台。
- 新增 HMAC-SHA256 签名的 `POST /api/v1/forwarding/inbound` 公共收件接口，校验时间窗口、域名、邮箱状态和原始 MIME 大小后复用现有邮件入库链路。
- 新增转发配置、密钥轮换、状态检测和域名邮箱列表 API/UI；签名密钥仅在管理员页面显示，支持脱敏、复制和轮换。
- 新增 `cloudflare/forwarding-worker.ts`、Wrangler 示例配置和部署说明，可选使用 `FORWARD_TO` 同时转发到外部邮箱。
- 增加重复投递去重、收件计数、最近收件时间和 Cloudflare Worker 入站测试，公网 TCP 25 被上游限制时仍可通过 HTTPS 收件。

### 域名邮箱 SMTP STARTTLS 收件修复（2026.08.20.1）

- 域名邮箱 SMTP 支持标准 `STARTTLS`，使用服务器证书加密公网 25 端口上的 SMTP 会话，兼容 Gmail、Outlook、ChatGPT 等发件方的 TLS 投递策略。
- Compose 增加只读证书目录挂载和证书/私钥路径环境变量；证书文件留在服务器部署目录，不进入镜像与 Git 仓库。
- SMTP 状态接口增加 `tls_enabled`，便于后台和排障确认当前容器是否已加载证书。
- 增加 STARTTLS 握手、加密会话投递和配置字段测试，并补充 Let's Encrypt 部署说明。

## 2026-08-19

### 域名邮箱 SMTP 系统设置可视化（2026.08.19.9）

- 将域名邮箱 SMTP 的启用开关、监听地址、容器端口和单封邮件大小纳入系统设置，管理员在后台保存后立即生效，无需编辑环境文件。
- 既有环境变量与 `config.json` 保留为首次启动兼容引导值，首次运行后设置持久化到 `data/state.json`。
- SMTP 服务支持后台即时启停和监听端口切换；Docker Compose 默认使用宿主机 TCP 25 映射到容器 2525，公网端口映射仍作为部署层配置保留。
- 域名邮箱状态、DNS 指引和系统设置接口同步展示实际 SMTP 运行状态，并补充配置校验与生命周期测试。
- Compose 默认镜像继续使用 `latest` 滚动标签并设置 `pull_policy: always`，服务器后续直接执行 `docker compose pull` 和重建容器即可获取最新发布版本，无需修改版本号。

### 商业化主题选择器（2026.08.19.8）

- 将“更多”菜单中的原生主题下拉框替换为自绘主题选择器，使用双列主题卡片、色板预览、当前选中标记和主题说明。
- 主题切换保留即时预览、浏览器自动保存与系统设置同步，公开接码页继续跟随管理员选定的主题。
- 主题浮层支持点击外部区域或按 Esc 自动关闭，并保留隐藏原生选择框以兼容已有配置与脚本。

### 商业化弹出菜单与全局主题同步（2026.08.19.7）

- “更多”菜单重构为账号卡、管理工具、界面偏好和版本状态四层商业化结构，补充图标、分组层级、浮层箭头与玻璃质感。
- “更多”、查看列和邮箱行操作浮层支持点击外部区域或按 Esc 自动关闭，避免菜单长时间遮挡业务内容。
- “查看列”改为双列字段管理面板，采用商业化开关、已启用列数、浏览器自动保存提示和独立恢复默认操作。
- 后台管理员选择的主题会持久化为系统主题，公开 HTML 接码页面从服务端读取并跟随该主题，新浏览器访问也保持一致。

### 侧边导航自适应紧凑布局（2026.08.19.6）

- 按视口高度同比缩小侧边栏宽度、品牌区、功能卡片、图标、字号和间距，释放更多业务内容空间。
- 侧边导航行高使用视口高度自适应范围，常见桌面高度下可完整展示全部功能入口。
- 移除侧边栏滚动条并隐藏滚动轨道，导航区域保持整洁稳定。

### 公开接码生命周期可配置开关（2026.08.19.5）

- 在“公开接码生命周期”中新增“启用公开接码生命周期”开关。
- 开启时按 HTML 地址有效期与到期清理策略执行；关闭时现有及新生成的公开接码地址均永不过期，自动过期清理不会处理这些地址。
- 系统设置 API、邮箱列表状态展示和公开页面验证逻辑同步支持该开关，旧配置默认保持开启。

### 工作台商业化重构与独立日志中心（2026.08.19.4）

- 为登录态、邮箱创建、域名邮箱和邮箱资产页补充统一的商业化信息层、指标卡和分区标题，保持矩龙邮箱自身视觉语言并保留原有操作接口。
- 日志从主页面底部拆分为独立“运行日志”导航页面，支持全部、运行、创建、定时分类，以及刷新、复制和清除操作。
- 日志页面与邮箱列表、取件码与发货页面互不干扰，页面切换状态可通过导航和地址参数恢复。

### 系统设置商业化策略工作台（2026.08.19.3）

- 重构管理员“系统设置”界面，采用策略总览、指标卡、分组卡片和固定保存栏的商业化工作台布局。
- 将访问与账户、邮件留存、公开接码生命周期、公开页面体验拆分为独立配置模块，补充开关说明、输入辅助文案和危险操作提示。
- 设置变更时实时更新策略摘要与指标卡，并显示“有未保存修改、正在保存、已保存、已同步”等状态，原有保存接口和字段保持兼容。

### 出库页独立检索与交付导出优化（2026.08.19.2）

- “全部邮箱”和“取件码与发货”改用独立搜索条件；在任一页面搜索、清空或点击出库批次都不会覆盖另一个页面的搜索结果。
- 搜索时“全部”分类标签、分类分组数量会展示当前搜索命中的数量，不再显示未筛选的总量。
- “取件码与发货”列表按邮箱最近更新时间倒序，新出库邮箱固定显示在最前；该页面的时间列改为“更新时间”，“全部邮箱”继续展示不可变的创建时间。
- 批量导出弹窗默认勾选“邮箱”和“HTML 地址”、默认 TXT；新增“复制选中/复制全部”按钮，按当前字段与格式直接写入剪贴板。

### 域名邮箱与双邮箱商业工作台（2026.08.19.1）

- 矩龙邮箱升级为 iCloud 隐私邮箱与自有域名邮箱统一管理平台；“全部邮箱”可辨识邮箱类型、所属 Apple 账号或所属域名，并复用现有邮件弹窗、HTML 接码链接和单邮箱取码 API。
- 新增“域名邮箱”工作台：接入、启停、删除域名，查看 SMTP 服务状态和 DNS 指引，并可批量生成可收件的域名邮箱地址。
- 新增内置 receive-only SMTP 服务：仅接收已启用域名下已生成的精确地址，解析纯文本与 HTML MIME 内容并写入现有邮件存储；域名邮箱取码仅读取本地已入库邮件，不触发 iCloud 同步。
- 域名邮箱删除只清理本地邮箱、邮件、API 和 HTML 接码链接；iCloud 隐私邮箱仍保持原有远端停用/删除流程。
- Docker 增加 SMTP 端口映射与环境变量，默认容器端口为 2525；新增域名生命周期、SMTP 入站、取码和管理 API/UI 测试。
- 域名收件、生命周期和 DNS 引导能力参考 CloakMail（MIT）的产品思路并以 Go 重新实现；继续保留原项目 `q1953258942/iCloud-Privacy-Mail` 的二开说明与署名。

## 2026-08-17

### Apple Account 分层保活与掉线复核（2026.08.17.1）

- 新接口保活改为两层状态机：先用现有动态 API Key 请求真实转发邮箱接口确认可用，再轻量触达 Portal bootstrap 续接浏览器 Cookie；仅在当前管理 token 被拒绝后执行隐私页面、Portal、管理 token 和动态 API Key 的完整恢复流程。
- 删除无认证意义的日志上报保活动作，避免每轮重复刷新整套管理 token；正常状态只发送轻量探测，降低请求密度和短时接口波动。
- HTTP 跳转期间会立即保存中间响应轮换的 Cookie、`scnt`、Session ID 和 DA Token，并把最新 Cookie 带到下一跳；识别跳转到登录入口或返回登录 HTML，避免把登录页误判成接口成功。
- Cookie 合并现在正确处理 `Max-Age` 删除和已过期 Cookie，防止服务端已撤销的旧 Cookie 覆盖仍可用状态。
- 单次网络错误或普通接口波动进入“保活恢复中”，保留最近 30 分钟内已确认可用的状态并在 10 秒后快速重试；401/403 认证异常连续复核两次后才标记登录态异常，恢复成功立即清零。
- 手动“检测登录态”与后台保活共用同一失败状态机，失败响应产生的新 Cookie 和会话头仍会持久化；前端在恢复期间显示警告状态和真实重试倒计时，不再把第一次瞬时失败直接显示成掉线。
- 新增轻量探测、深度刷新、登录跳转识别、跳转 Cookie 保存、过期 Cookie 删除、短暂故障健康宽限和连续认证失败确认测试。

## 2026-08-16

### Apple Account 主会话续期与快速恢复（2026.08.16.14）

- 新接口保活每轮先访问 Apple Account 隐私页面与 Portal bootstrap，再刷新管理 token、动态 API Key、转发邮箱和轻量日志接口；由只触达管理 API 改为同时续期浏览器 Portal 主会话。
- 管理 token 首次返回 401/403/419 时，不再立即判定登录态失效；系统会保留失败响应返回的新 Cookie、`scnt` 和 Session ID，预热 Portal 后依次尝试无旧 `scnt` 恢复与原 `scnt` 回退。
- 管理 API 与 Portal 的非 2xx 响应现在也会先写回服务端轮换的 Cookie 和会话头，避免可用于下一次恢复的凭据被丢弃。
- Docker 与默认配置的正常保活间隔由约 4 分钟缩短为约 2 分钟，单轮超时由 25 秒放宽到 60 秒；短 TTL 会在到期前约 2 分钟提前刷新。
- 失败恢复改为 30 秒、1 分钟、2 分钟、4 分钟、最多 5 分钟的指数退避；前端倒计时使用真实重试时间，并在悬停说明中展示连续失败次数，恢复成功后自动清零。
- 新增 Portal 触达、过期 token 自动恢复、失败响应凭据保留、快速退避、成功清零、TTL 自适应与前端重试时间测试。

### 出库批次、联合搜索与按需显示列（2026.08.16.13）

- “全部邮箱”批量出库弹窗新增批次输入；批次留空时由后端为同一次操作统一生成当前 13 位毫秒时间戳，避免多邮箱落入不同批次。
- 邮箱数据新增持久化出库批次字段，“取件码与发货”新增批次列；点击批次可直接生成对应的批次搜索条件，退回库存时自动清除原出库批次。
- 搜索框支持联合条件：`[批次]` 查找包含该批次的邮箱、`![批次]` 排除该批次、`[已激活]` 查询 HTML 已激活邮箱、`![已激活]` 排除已激活邮箱，使用 `&&` 组合多个条件。
- 联合搜索同时支持未激活、已过期、邮箱状态及 API 启停关键字；普通邮箱、标签、账号搜索保持原有行为，并将批次加入普通搜索范围。
- 新增“查看列”下拉菜单，可按需显示或隐藏 ID、标签、状态、邮箱、Apple 账号、批次、邮件、创建时间、HTML 过期时间、取件码和操作列，配置保存在当前浏览器。
- 批量出库改为单次服务端操作，最多支持 5000 个邮箱并统一写入批次；新增批次权限、时间戳兜底、联合搜索、列选择和界面静态测试。

### Apple Account 新接口异常后持续保活（2026.08.16.12）

- 修复新接口保活遇到一次临时网络错误或 Apple 响应异常后被永久移出保活队列的问题；只要登录态仍保存 `scnt` 与动态 API Key，后台就会按原保活周期持续自动重试。
- 每次保活失败都会保存最近检测时间、错误状态和服务端返回的最新登录凭据，同时保留原 Cookie 与 API Key，避免临时错误导致可恢复状态丢失。
- 登录态异常时前端由“新接口保活：已停止”改为“新接口重试”倒计时；后续保活成功会自动恢复“新接口登录态正常”和正常保活倒计时，无需因单次失败立即重新登录。
- 新增自动恢复、普通网络错误写回、凭据保留、失败状态重试时间和前端状态提示测试。

### 出库邮箱 HTML 状态筛选与过期清理（2026.08.16.11）

- “取件码与发货”新增 HTML 状态筛选，可查看全部、已激活、未激活和已过期邮箱；筛选在服务端分页前执行，分页、总数和 Apple 账号分组同步反映筛选结果。
- 到期 HTML 地址会保留“已过期”状态，不再自动替换成新的未激活地址；访问旧地址继续返回过期页面，邮箱 API token 继续保持有效。
- 新增“清理全部已过期”操作，使用项目自绘确认框，逐个永久删除当前数据范围内已出库且 HTML 地址过期的 iCloud 邮箱。
- 过期清理与单邮箱删除、自动到期删除共用“远端成功或确认不存在后再删除本地数据”的流程；远端失败时保留邮箱、邮件、API 和 HTML 地址。

### 邮箱列表表头层级修复（2026.08.16.10）

- 修复“全部邮箱”和“取件码与发货”共用表格滚动时，首行内容绘制到固定表头上方的问题。
- 为邮箱表格滚动容器建立独立层叠上下文，并为表头设置稳定的层级和背景，滚动内容始终位于表头下方。
- 保持邮箱查询、批量操作、出库和导出等既有功能不变。

### 根域名静态欢迎页（2026.08.16.9）

- 未登录访问根域名时不再跳转到后台入口，避免通过重定向响应暴露管理员设置的登录/注册路径。
- 根域名新增独立静态欢迎页，只展示矩龙邮箱 Logo、项目名称和“欢迎使用矩龙邮箱”文案，不包含入口链接、登录按钮或跳转脚本。
- 已登录用户访问根域名仍进入原有管理工作台，邮箱创建、同步、取码和系统设置功能保持不变。
- 自动化测试覆盖欢迎页不包含默认或自定义入口、旧认证地址保持 404、正确入口仍可登录与注册。

### 唯一登录与注册入口（2026.08.16.8）

- “后台入口路径”升级为唯一登录/注册入口；例如保存 `/julongyx` 后，登录页、登录请求和注册请求都只接受该路径。
- 标准 `/login`、`/api/auth/login`、`/api/auth/register`、`/api/auth/register-status` 入口停止提供认证功能，修改入口后旧路径立即失效。
- 未登录访问首页会跳转到当前配置入口；已登录访问该入口继续进入账号数据管理页，原有工作台和管理功能保持不变。
- 登录页根据当前访问路径提交登录/注册，注册开关状态也从同一路径读取；登录态失效后前端统一返回首页并跳转到当前入口。
- Docker Compose 健康检查改为访问首页，不再依赖固定登录地址。

### HTML 接码邮件刷新间隔（2026.08.16.7）

- “系统设置”新增“HTML 接码邮件自动刷新秒数”，默认 20 秒，可配置范围为 5-3600 秒。
- 公开 HTML 接码页从数据接口读取管理员设置，无需重新生成地址；管理员保存新间隔后，已打开页面会在下一次刷新时自动采用。
- 页面改为上一轮请求结束后再安排下一次刷新，避免网络延迟时产生重叠请求；每秒倒计时仍只在浏览器本地执行。
- HTML 数据接口新增 `refresh_seconds` 字段，旧数据自动使用默认 20 秒。

### HTML 接码页完整倒计时（2026.08.16.6）

- 公开 HTML 接码页右上角的剩余时间由仅显示小时改为完整显示天、时、分、秒。
- 倒计时每秒更新一次，邮件数据仍保持每 10 秒自动刷新，减少无关网络请求。
- 每次邮件数据刷新都会用服务端剩余秒数重新校准倒计时，避免浏览器计时偏差持续累积。
- 剩余时间归零后直接显示“已过期”。

### HTML 有效期改为秒并实时预览（2026.08.16.5）

- “HTML 接码地址过期天数”改为“HTML 接码地址过期秒数”，支持 1-315360000 秒。
- 输入秒数时实时预览对应的“天、时、分、秒”，例如 `93784` 秒显示为 `1天 2时 3分 4秒`。
- 新生成且首次访问的 HTML 地址按秒计算到期时间，适合设置几秒、几分钟或几小时的短有效期。
- “全部邮箱”的未激活有效期和激活后剩余时间同步改为天、时、分、秒显示。
- 已有按天保存的系统设置会自动换算为秒；已经激活并生成准确到期时间的链接保持原到期时间。

### HTML 页面邮件展示数量（2026.08.16.4）

- “系统设置”新增“HTML 页面邮件展示数量”，管理员可设置每个公开 HTML 接码页展示最近多少封邮件。
- 默认继续展示最近 50 封，可配置范围为 1-500 封，旧数据自动使用默认值。
- HTML 数据接口返回实际展示上限，并按收件时间倒序截取邮件。
- 最新验证码会在页面截取前从该邮箱最近 30 天的已保存邮件中查找，不受展示数量影响；即使最新普通邮件占满展示位置，仍会返回最近验证码。

### 邮件保留与 HTML 过期删邮箱开关（2026.08.16.3）

- “系统设置”新增“只保留验证码邮件”开关；默认继续只保存验证码邮件，关闭后 IMAP 与旧接口同步会保存该隐藏邮箱收到的全部邮件。
- 保存全部邮件时，“最新验证码”、公开 HTML 接码页的最新码和取码 API 仍只从含验证码的邮件中提取结果，普通通知不会被当成验证码返回。
- “系统设置”新增“HTML 接码地址过期后自动删除邮箱”开关；开启后，后台任务每分钟检查已激活且到期的地址。
- 自动删除会先永久删除对应的 iCloud 隐私邮箱，远端成功或确认已不存在后才清理本地邮箱、邮件、API token 和 HTML 地址；登录态缺失或远端失败时保留数据并在后续轮次重试。
- 旧数据默认保持验证码过滤开启、HTML 过期删邮箱关闭；关闭自动删除时继续沿用过期 HTML 地址清理并补发新地址的原行为。

### Docker Hub 默认镜像修正（2026.08.16.2）

- Compose 默认镜像改为 `docker.io/qq1371446705/julong-ic-email:latest`。
- 移除 GHCR 上传和 GitHub Packages Token 流程，统一使用项目已有的 Docker Hub 账号构建和拉取。
- 服务器拉取仓库后无需额外指定镜像地址，直接执行 `docker compose pull` 即可。
- README 和发布说明同步更新本地构建、多架构推送及服务器启动命令。

### Docker Compose 预构建镜像部署（2026.08.16.1）

- 新增多阶段 `Dockerfile`，本地可构建 Linux amd64/arm64 容器镜像。
- 新增 `compose.yaml`，服务器直接从 Docker Hub 拉取预构建镜像，不执行 Go 源码编译。
- 新增 Docker 专用无敏感配置、Compose 环境变量模板、健康检查和持久化数据卷。
- `.dockerignore` 排除运行数据、本地配置、密钥、日志和构建产物，避免进入镜像上下文。
- README 增加本地构建推送、服务器拉取启动和后续更新命令。

## 2026-08-15

### TXT 出库导出格式调整（2026.08.15.17）

- “取件码与发货”的 TXT 导出取消表头，每个邮箱固定占一行。
- 所选的邮箱、API、HTML 地址按界面顺序使用 `----` 连接，例如 `邮箱----HTML地址`。
- TXT 内容仍保持 UTF-8 编码，换行、制表符会先转换为空格，避免破坏记录结构。

### 出库邮箱导出格式选择（2026.08.15.16）

- “取件码与发货”的全部导出和批量导出弹窗新增 TXT、CSV、JSON 文件格式选择。
- TXT 保持每行一条邮箱记录，CSV 保留 UTF-8 表头，JSON 输出结构化记录数组。
- 保存文件名、MIME 类型和系统文件选择器会根据所选格式自动切换。
- 新增三种格式入口与生成函数的模板源代码测试。

### 全部邮箱按创建时间倒序（2026.08.15.15）

- “全部邮箱”的“更新时间”列改为“创建时间”，只展示邮箱首次入库时写入的 `created_at`。
- 邮箱列表排序改为创建时间倒序，最新新增的邮箱始终排在最前面。
- 同步邮件、修改状态、出库、API 启停等后续操作继续维护内部更新时间，但不会改变创建时间或展示顺序。
- 新增创建时间表格来源、最新优先排序和状态更新后创建时间保持不变的测试。

### 取件码与发货全部/批量导出（2026.08.15.14）

- “取件码与发货”工具栏新增“全部导出”，批量选择栏新增“批量导出”。
- 导出按钮使用项目自绘弹窗，可多选邮箱、API、HTML 地址三类导出内容。
- 全部导出覆盖当前搜索和 Apple 账号筛选下的全部分页；批量导出只包含跨页选中的邮箱。
- 导出文件使用带 UTF-8 BOM 的 CSV 格式，可直接使用 Excel 打开，并显示导出数量、字段和结果提示。
- 新增导出入口、弹窗、字段选择、跨页加载和 CSV 生成的模板源代码测试。

### 全部邮箱批量出库与发货列表（2026.08.15.13）

- “全部邮箱”批量操作栏新增“出库”按钮，确认后将选中邮箱状态统一改为“出库”。
- 侧边栏“取件码与发货”改为独立出库列表；库存列表自动排除出库邮箱，出库列表只展示出库邮箱。
- 出库邮箱继续保留邮件查询、验证码、API 和 HTML 地址等原有功能，并支持单个或批量退回库存。
- 邮箱列表接口新增 `status` 与 `exclude_status` 筛选，状态接口新增库存和出库数量，导航栏分别显示两类计数。
- 新增出库状态更新、列表筛选、账号隔离、状态计数和前端工作流源代码测试。

### 全部邮箱单邮箱邮件查询弹窗（2026.08.15.12）

- “全部邮箱”表格中的邮件数量改为可点击按钮，点击后查询并显示当前邮箱自己的邮件列表。
- 邮件弹窗展示主题、发件人、收件时间、正文和识别到的验证码，验证码支持点击复制。
- HTML 邮件通过隔离沙箱可视化预览，脚本、表单、跳转和外部资源继续受限。
- 弹窗支持同步当前邮箱、刷新邮件列表、遮罩关闭和 `Esc` 关闭，并适配全部主题与移动端。
- 单邮箱邮件接口按收件时间倒序返回，并增加邮箱隔离、排序及弹窗源代码测试。

### 统一项目 Logo 与网站 favicon（2026.08.15.11）

- 使用用户提供的蓝色松鼠信封图片生成 `internal/app/templates/logo.png` 与多尺寸 `favicon.ico`。
- 首页、登录页、管理后台、公开 HTML 接码页和过期页统一引用 `/logo.png` 与 `/favicon.ico`。
- Go 服务新增内嵌静态资源路由，并覆盖资源响应头、文件签名和全部模板 favicon 引用测试。

### 发布仓库与构建资产命名统一（2026.08.15.10）

- 在线更新 manifest 示例中的构建资产统一改为 `julong-ic-email_linux_amd64`，与项目英文名、发布说明和 GitHub Release 约定保持一致。
- 本次变更继续按 `AGENTS.md` 约定提交并推送到 `Xujs98/julong-ic-email`。

### 二开项目标识与 GitHub 自动推送约定（2026.08.15.9）

- 项目正式命名为“矩龙邮箱（julong-ic-email）”，Go module、服务标识、构建文件名、部署目录和在线更新仓库同步使用新名称。
- README 与发布说明注明本项目基于 `q1953258942/iCloud-Privacy-Mail` 二次开发，当前维护仓库改为 `Xujs98/julong-ic-email`。
- 根目录新增 `AGENTS.md`，规定每次代码完成且验证通过后必须提交并推送到 GitHub；首次推送失败后再重试三次。
- 新增 `scripts/push-with-retry.sh`，统一执行首次推送和最多三次失败重试。
- `.codex-artifacts/` 加入忽略列表，避免把本地验证日志、临时二进制和运行探针提交到公开仓库。

### 邮箱标签与 HTML 有效期天数（2026.08.15.8）

- “全部邮箱”表格补回“标签”列，直接展示创建隐私邮箱时填写的标签，空标签显示“未设置”。
- HTML 地址尚未访问时显示“未激活”，并展示首次激活后可使用的天数。
- HTML 地址首次访问后显示“已激活”和剩余天数，鼠标悬停继续提供激活时间与准确到期时间。
- 邮箱列表接口新增 `html_link_ttl_days` 字段，并增加测试覆盖邮箱标签、默认有效期和系统设置后的有效期展示数据。

### 全局主题询问框（2026.08.15.7）

- 首页和账号数据后台新增统一的矩龙邮箱自绘询问框，替换项目内全部浏览器原生确认框。
- 更新程序、批量停用 API、清理远端邮件、永久删除邮箱和删除网站账号均使用新的异步确认组件。
- 询问框根据普通、警告和危险操作切换主题色、图标、说明与确认按钮，并适配十套明暗主题和移动端底部弹层布局。
- 支持取消按钮、点击遮罩取消、`Esc` 取消、`Enter` 确认以及关闭后恢复原焦点。

### 邮箱 API 启停切换（2026.08.15.6）

- “全部邮箱”操作菜单会根据邮箱 API 当前状态动态显示“停用 API”或“启用 API”。
- 停用 API 后无需再通过状态选项绕行，可直接在同一菜单重新启用。
- 重新启用会保留 iCloud 地址状态，并将 API 停用状态恢复为可用；操作完成后显示浮动提示并刷新表格。
- 新增接口测试，覆盖停用、重新启用、API 状态和邮箱状态恢复。

### iCloud 邮箱永久删除（2026.08.15.5）

- “全部邮箱”的删除按钮不再只清理本地 API 绑定，而是先在 iCloud 查询对应 Hide My Email 地址。
- 活跃地址先调用停用接口，再调用永久删除接口；远端确认删除或地址已不存在后，才删除本地邮箱、邮件、API token 和 HTML 地址。
- 远端删除失败、登录态缺失或 iCloud 未返回远端 ID 时保留全部本地数据，并在页面显示失败原因。
- 单个和批量删除统一使用永久删除确认文案；批量操作按顺序执行并显示成功/失败数量。
- 新增测试覆盖活跃地址停用后删除、远端地址已不存在、本地清理成功，以及远端失败时保留本地数据。

### HTML 地址首次使用激活（2026.08.15.4）

- 邮箱入库时自动生成独立 HTML 接码地址，生成后保持“待首次使用”，不会提前消耗有效期。
- 第一次打开 `/mailbox/{token}` 页面或请求其 `/data` 接口时才记录激活时间，并按系统设置计算过期时间；重复访问不会续期。
- 过期 HTML token 清理后会为原邮箱补发新的未激活地址；邮箱 API token、API 状态和邮件数据保持不变。
- “全部邮箱”表格新增“HTML 过期时间”列，分别展示待首次使用、已激活到期时间和过期状态。
- 邮箱、API、HTML 地址、验证码及对应批量复制操作新增浮动成功提示。
- 新增测试覆盖入库自动生成、首次访问激活、重复访问不续期、列表过期字段和过期后自动补链。

### HTML 邮件可视化预览（2026.08.15.3）

- HTML 接码页的最近邮件新增 HTML/纯文本自动识别，不再把邮件标签和 CSS 源码作为正文直接显示。
- IMAP 与 iCloud 邮件同步会同时保存可检索纯文本和原始 HTML，验证码提取继续使用纯文本，公开页面使用 HTML 排版预览。
- HTML 邮件在无脚本沙箱中渲染，并移除脚本、表单、内嵌页面、跳转和外部资源；纯文本邮件保持原展示方式。
- 历史邮件若只剩文本与 CSS 残留，页面会先清理常见样式声明再展示。

### 全部邮箱表格与批量操作（2026.08.15.2）

- “全部邮箱”按参考表格结构重做，统一展示 ID、状态、邮箱、Apple 账号、邮件数、更新时间、取件码和操作列。
- 邮箱地址改为可点击复制，并保留验证码预览、单邮箱同步、HTML 接码、复制 API、状态修改、远端邮件清理、API 停用和删除等原有操作。
- 新增当前页全选、跨分页选择和选中行高亮，批量栏实时显示已选数量。
- 新增批量复制邮箱/API、生成 HTML 接码地址、同步邮件、修改状态、停用 API 和删除；网络操作使用有限并发执行。
- 表格和批量栏继续使用现有主题变量，适配全部十套明暗主题和窄屏横向滚动。

### 矩龙邮箱界面与公开接码页

- 项目与全套页面统一使用“矩龙邮箱”品牌，并完成十套明暗主题适配。
- 邮箱列表新增“HTML 接码”，可生成并复制 `/mailbox/{token}` 单邮箱公开页面，自动展示最新验证码、最近邮件和剩余有效时间。
- HTML token 与邮箱 API token 分离；HTML 地址按系统设置过期并自动清理，原 API 地址继续保持原有长期有效规则。
- 侧边栏新增“系统设置”，管理员可设置注册开关、后台入口路径以及新生成 HTML 接码地址的有效天数。
- 后台入口修改后立即生效，并保留 `/manage` 兼容入口；关闭注册后登录页同步禁用注册选项。
- 新增测试覆盖 HTML 地址生成与公开访问、注册关闭、动态后台入口，以及过期 HTML token 清理不影响邮箱 API。

## 2026-07-02

### 发布边界

- 明确公开版发布规则：每次修复功能、问题或公开行为，都必须提升 `AppVersion`、提交 Git，并发布新的 GitHub Release。
- 在线更新只读取最新 Release 或 manifest 最新版本；用户即使跳过中间多个版本，也会直接更新到最新版本，不需要逐个版本升级。
- Release 资产只包含程序二进制和校验文件，不包含 `data/state.json`、配置、登录态、Cookie、验证码缓存或服务器私有数据。

### 版本提示

- 版本号悬浮提示改为只显示本次更新内容，不再显示当前版本、最新版本、发布时间、资产名、构建提交等辅助信息。
- 对已有 Release notes 做前端过滤：只保留“本次公开版更新”段落，自动隐藏“资产”“构建提交”等段落。

### 在线更新与版本展示

- 前端顶部新增当前版本显示；鼠标悬停只展示本次更新内容。
- 新增 `/api/update/status` 检查更新接口，默认读取公开 GitHub 最新 Release，也支持配置 `update_manifest_url` 使用自定义 manifest。
- 当 GitHub 仓库还没有发布 Release 时，不再把 `releases/latest` 的 404 显示成检查失败；后端会退回读取默认分支最新 commit，用当前二进制内置 commit 判断是否已经是最新源码。
- 管理员可在检测到可用二进制资产后点击“更新”，服务会下载当前系统架构的程序文件并替换当前二进制；`data/state.json` 不参与更新，不覆盖用户数据。
- 更新配置新增 `update_enabled`、`update_repository`、`update_manifest_url`、`update_asset_name`，适合公开版部署者无需重新拉源码即可升级。

### Apple Account 保活展示

- 新接口登录态已检测异常时，前端不再继续显示下一次保活倒计时，改为显示“新接口保活：已停止”。
- 后台新接口保活 worker 会跳过已检测异常的登录态，避免异常状态下继续保活并造成前端误判；手动检测成功或重新登录后才会重新进入保活队列。

## 2026-07-01

### Apple Account 新接口保活

- 根据长时间浏览器 HAR 对比确认：`/v2/jslogs` 会持续返回新的浏览器会话标识，但不能证明隐私邮箱管理接口仍可用；真实入口 `/account/manage/email/private` 和 `/account/manage/forwardemail` 仍可能在页面保持打开时返回 401。
- 后台新接口保活改为随机错峰触发真实管理接口：刷新 `gs/ws/token`、重新读取 `/account/manage` 动态 `apiKey`，再轻量请求 `/account/manage/forwardemail` 验证管理态仍可操作。
- 后台保活 worker 使用短周期扫描，到点的账号才执行保活；不同账号按 owner/account/Apple ID 生成稳定随机偏移，避免所有账号同一秒打到 Apple。
- `/v2/jslogs` 仅作为补充同步浏览器会话头，不再作为新接口登录态正常的唯一依据。
- “检测登录态”现在会触达真实管理接口，避免只刷新时间但点隐私邮箱入口仍掉登录态的误判。
- 前端新接口倒计时显示改为后端下一次保活时间，不再由前端循环自动刷新生成。
- 新增测试覆盖真实管理接口保活请求顺序和后台保活写回已保存登录态。

### Apple 登录验证码方式

- 新接口登录和旧接口登录都支持选择 2FA 验证码方式：受信任设备或短信/手机号验证码。
- 登录验证码方式纳入服务器端创建配置，刷新页面后仍会保留，不再只存在浏览器本地。
- 登录按钮文案从“保存新/旧接口登录态”精简为“新接口登录 / 旧接口登录”。
- 新增测试覆盖验证码方式配置保存，以及新接口短信验证码提交链路。

### 隐私邮箱同步

- “同步已有邮箱”继续固定使用 iCloud Web 旧接口 `/v2/hme/list`，新接口只用于创建邮箱，不参与同步已有邮箱。
- 全部登录态同步改为账号级并发隔离；某个账号卡住或超时只会记录该账号失败，不再拖到整次请求 504。
- 同步结果会记录每个账号实际使用的旧接口和失败原因，避免只显示笼统的“同步失败”。
- 根据 iCloud CN 抓包确认旧接口已有邮箱列表仍为 `GET /v2/hme/list`，并将旧接口默认前端 build 号更新到 `2622Build20`；已保存 build 的登录态不受影响。
- 新增测试覆盖旧接口列表解析和慢账号隔离，确保一个账号超时不影响其他账号同步入库。

### 公开取码 API

- 后台取码同步器接入 IMAP `IDLE` 常驻监听，收到 `EXISTS` 新邮件事件后立即触发同步入库。
- IMAP 同步改为优先使用取码登录态的账号级 `IMAPLastSyncUID` 做 `UID n:*` 增量搜索，兼容旧邮箱级 `LastSyncUID`，没有 UID 游标时才按日期回看兜底。
- IMAP 监听同步不再为没有新验证码的空邮箱逐个写入同步游标，避免大邮箱池反复刷写 `state.json` 堵住公开取码 API。
- 后台取码监听启动时会先读取 iCloud IMAP 当前 `UIDNEXT` 并保存为账号级起跑线，之后只同步新进来的邮件，不再启动就翻历史邮件。
- IDLE 监听 worker 不再因为邮箱同步游标更新而重启，避免刚入库就断开常驻监听。
- 大邮箱池同步前会先构建取码登录态索引，避免每个邮箱重复查找/克隆登录态导致同步上下文被耗尽。
- IMAP 连接改为 TCP 公共 DNS 优先，并以 UDP DNS、本机 resolver 和 Apple IMAP IPv4 直连兜底，降低服务器 DNS/53 端口偶发超时对取码同步的影响；直连仍使用 `imap.mail.me.com` 做 TLS SNI。
- 原 3 秒后台轮询保留为兜底，防止 IMAP 事件漏掉或连接被 Apple 断开。
- 修复公开取码 API 等待补抓时丢失请求 `keyword` 的问题；外部传入 `keyword=ChatGPT` 时，后端会按 ChatGPT 同步验证码邮件，不再固定用 OpenAI。
- 新增面板取码预览模式；后台面板点击“验证码”只用于查看和复制，不会占用公开 API 的已发放验证码记录。
- 修复取码请求等待期间后台监听已把验证码入库、但本次请求没有再次读取本地缓存导致第一次返回空、第二次才秒出的竞态；等待过程中会实时轮询本地入库结果，发现验证码立即返回。
- 面板“验证码”按钮不再使用浏览器本机时间生成 `after` 参数，避免客户端时间偏差或 5 分钟边界把服务器已有验证码过滤掉。
- 新增测试覆盖 ChatGPT keyword 补抓、面板预览不消耗公开 API、IMAP EXISTS 事件识别和 UID 增量搜索。

## 2026-06-30

### 验证码读取提速

- 服务启动后默认启用后台取码同步器，按 Apple 账号批量同步活跃隐私邮箱的验证码邮件。
- 后台取码同步器默认 3 秒一轮，启动时预抓最近 24 小时内的最近 20 个邮件线程，后续每轮扫描最近 8 个线程。
- 对外取码 API 改为本地优先；本地未命中时触发后台快速补抓，默认最多等待 600ms，补抓继续在后台完成。
- 取码同步继续复用原有 iCloud Mail WebService、同步游标、owner 级同步锁和同步最小间隔，避免高频轮询打爆 Apple 接口。
- 新增 `MAIL_WATCHER_*` 和 `PUBLIC_*SYNC*` 配置项，公开说明里补充“后台先入库、查询读本地”的提速模型。
- 新增测试覆盖 watcher 配置读取、启动预抓写入验证码、本地未命中时快速返回且后台继续同步。

### 新接口登录态保活与刷新

- 修复 Apple Account 新接口短效管理态被当成长效状态复用的问题。
- 新接口会按 Apple 返回的管理态 TTL 到期时间刷新 `scnt`、Cookie 和动态 `apiKey`，再使用刷新后的状态创建。
- 新接口创建失败时只保存最后一次成功响应得到的刷新态，非 2xx 失败响应不再覆盖 `scnt` 或 Cookie。
- 新接口浏览器指纹调整为抓包一致的中文语言和东八区时区，减少管理态检测与创建接口的指纹漂移。
- 新接口 401/403 不再一律判定为登录态失效；只有明确返回 authentication/session 失效语义时才要求重新登录，普通 301/404/500/503 会保留原始 HTTP 错误。
- 新接口创建与登录态检测按同一个账号串行执行，避免检测刷新 `scnt` 时创建接口同时使用旧状态。
- 新接口达到隐私邮箱创建上限时会进入短冷却，当前轮回落旧接口或跳过该账号，下一轮再重新尝试。
- 登录前获取 Apple Account 挑战 `scnt` 与登录后管理态保存拆开处理，避免挑战响应污染已保存的新接口登录态。
- 新接口 2FA 通过后会尝试补 Apple 信任确认；失败时不阻断正常保存，只在调试模式输出脱敏摘要。
- 明确说明浏览器 “Remember me” 不等同于旧 iCloud Web 长效 Cookie；旧接口长效来自 `2sv/trust` 和 `extended_login` 换取的 iCloud WebServices Cookie。

### 登录态检测与展示

- 登录态检测同时覆盖 Apple Account 新接口和 iCloud Web 旧接口。
- 新接口登录态检测会复用近期健康状态，避免刚保存的新接口状态被检测刷新误判为异常。
- 每个 Apple 登录态会分别展示“新接口”和“旧接口”的登录状态。
- 前端卡片精简为 Apple ID、账号 ID、新旧接口状态和整体登录态结果。
- Apple ID / 账号邮箱在前端全量显示，不再用星号脱敏。
- 去掉前端卡片里多余的最近检测时间、Cookie 数量、DSID、保存时间等调试信息。
- 新增测试覆盖新接口登录态检测、公开状态字段和新旧接口状态拆分。

### 管理面板 UI

- 管理员后台改为新的分类化数据面板，顶部动作区和数据卡片重新整理。
- 首页提供清透、蓝调、灰阶、舒展、紧凑等前端风格切换组件。
- 账号信息与“账号数据/退出”入口移动到右上角，减少左侧栏拥挤。
- 导出和清理工具从左侧栏移动到“高级工具”分类中，不再常驻挤占导航。
- 日志区域统一展示运行日志、创建账号日志和定时创建日志；切换普通分类时日志保留，切到邮箱池时隐藏。

### 隐私邮箱创建

- 隐私邮箱创建优先使用 Apple Account 新接口。
- 新接口创建前会优先使用近期可用的 `scnt` 和动态 `apiKey`，真正认证失败时再刷新并重试。
- 新接口创建失败后，会继续尝试 iCloud Web 旧接口。
- 新接口与旧接口的创建上限冷却分开记录，新接口达到上限不会把旧接口也判定为冷却中。
- 定时创建按账号并行处理；某个账号本轮失败后，只跳过该账号当前轮次，其他账号继续创建。
- 定时创建只保留定时间隔配置，默认 60 分钟；每轮创建间隔可自定义，默认 5 秒。
- 普通创建和定时创建都支持选择“自动使用新旧接口 / 只用新接口 / 只用旧接口”。
- 创建配置保存到服务器状态文件，刷新页面或换浏览器后仍会保留账号选择、接口策略和定时间隔。
- 定时创建记录已创建数量、失败数量、下一次执行时间和最近错误。
- 定时创建记录支持复制日志和清除日志；清除只清记录，不重置创建/失败计数。
- 创建账号列表里会完整显示 Apple ID，并清楚标记新接口、旧接口和整体登录态状态。
- 创建区域新增“检测登录态”入口，可直接刷新新旧接口状态显示。
- 创建失败会展示账号级失败原因；Apple 新接口错误会带阶段、HTTP 状态码和脱敏后的原始返回摘要，方便判断是接口入口异常、登录态失效、额度上限还是服务端临时错误。
- Apple 新接口 2xx 空响应按临时失败处理，下一轮优先重试新接口，再次失败才回落旧接口继续。
- Apple 新接口遇到 DNS 超时、连接超时、连接重置等底层网络错误时会自动重试 3 次；HTTP 业务错误、额度上限和登录态失效不走网络重试。
- 新接口刷新倒计时到点后会自动调用登录态检测接口，真正刷新 Apple Account 管理态时间，不再只刷新页面状态。
- 登录态检测返回“新接口正常”但未返回刷新倒计时时，会自动继续检测，直到拿到刷新时间或该登录态转为异常。
- 多账号自动刷新改为逐账号判断；某个账号仍显示“待自动刷新”时会继续刷新，不会被其他账号已拿到倒计时而提前停止。
- 新接口刷新倒计时到点后的自动检测节奏调整为每 3 秒一次，避免短时间连续请求。

### 前端与数据管理

- 前端改为左侧分类导航，按分类查看数据。
- 隐私邮箱数据支持分页，默认每页 10 条，并支持自定义每页数量。
- 用户、登录账号、隐私邮箱等数据拆分到对应分类中显示。
- 管理员可以删除普通用户注册的账号。
- 邮箱 API 复制格式调整为 `邮箱----API`。

### 公开版整理

- 清理公开仓库说明，避免提交真实运行数据和私有部署信息。
- `dist/`、`发布目录/`、`data/`、`config.json` 等运行或发布产物默认不入库。

### 测试与发布

- 新增 live Apple Account 登录态保存与检测测试；测试会从仓库根配置读取真实 `data_path`，避免误写包目录临时数据。
- 新增测试覆盖新接口创建前刷新、创建失败仍保存刷新态、2FA 后信任确认尝试。
- 新增测试覆盖新接口错误日志原始返回、检测误判后的近期状态复用、定时创建每轮间隔配置。
- 发布说明补充标准升级方式、回滚方式和公开版安全边界。
