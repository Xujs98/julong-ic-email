# 矩龙邮箱（julong-ic-email）发布与使用说明

> **2026.08.19.6 更新**：侧边导航按视口高度缩小宽度、卡片、图标和间距，桌面端完整展示全部功能入口并移除侧边滚动条。

> **2026.08.19.5 更新**：公开接码生命周期新增启用开关；关闭后 HTML 接码地址永不过期，开启后继续按有效期与到期清理策略执行。

> **2026.08.19.4 更新**：工作台其他业务页统一升级为商业化布局，并新增独立“运行日志”页面；邮箱列表、域名资产和创建工作台增加状态指标与分区信息层。

> **2026.08.19.3 更新**：管理员“系统设置”升级为商业化策略工作台，新增策略摘要、指标总览、分组配置卡片、实时变更状态和固定保存栏；原有配置字段与保存接口保持兼容。

> **二开说明**：本项目由 Xujs98 基于 `q1953258942/iCloud-Privacy-Mail` 二次开发，代码与后续版本维护在 `Xujs98/julong-ic-email`。

## 1. 这是什么

这是一个 Go 单文件后端项目，前端页面已经通过 `embed` 打进程序里。

解压后可以直接二开，也可以直接编译运行。

仓库已提供 `Dockerfile`、`compose.yaml`、`config.docker.json` 和 `.env.example`。Docker 部署采用本地构建并推送到 Docker Hub 的 `qq1371446705/julong-ic-email`、服务器只执行 `docker compose pull` 和 `docker compose up -d` 的方式，服务器不参与源码编译。

“全部邮箱”使用表格化布局，展示每个邮箱的创建时间和 HTML 过期时间，并按创建时间倒序排列；创建时间只在邮箱首次入库时记录，后续同步、状态更新、出库和邮件操作不会改变该列。“取件码与发货”使用独立搜索条件，按最近更新时间倒序展示已出库邮箱，时间列显示“更新时间”，新出库邮箱固定在最前。两个页面互不覆盖搜索条件；搜索时“全部”和分类标签显示当前命中数量。点击邮箱、API、HTML 地址或验证码会复制并显示提示，勾选邮箱后可批量复制邮箱/API/HTML 地址、同步邮件、出库、修改状态、停用 API，或从 iCloud 永久删除邮箱。

Apple Account 新接口默认约每 2 分钟先续期 Portal 主会话，再刷新管理 token、`scnt`、Cookie、Session ID 和动态 API Key，并触达真实管理接口。401/403/419 响应返回的新会话凭据会先保存，再自动执行 Portal 预热、无旧 `scnt` 恢复和原 `scnt` 回退；临时失败按 30 秒、1 分钟、2 分钟、4 分钟、最多 5 分钟快速重试，后续成功后自动恢复正常状态。Apple 服务端主动撤销、密码变更或安全策略触发的会话仍需重新登录。

批量点击“出库”后，邮箱状态会改为“出库”并从库存列表移入侧边栏“取件码与发货”。出库列表保留邮箱邮件、验证码、API 和 HTML 地址查询能力，并支持批量退回库存。

出库弹窗可为所选邮箱填写同一个批次；留空时后端统一生成当前 13 位时间戳。“取件码与发货”新增批次列，点击批次可直接搜索；搜索框支持 `[批次]`、`![批次]`、`[已激活]`、`![已激活]` 和使用 `&&` 连接的组合条件。表格“查看列”菜单可按需隐藏或显示各业务列，并在当前浏览器保存选择。

“取件码与发货”提供“全部导出”和“批量导出”：全部导出会读取当前搜索、Apple 账号筛选下的全部分页，批量导出只处理已选择邮箱。导出弹窗默认选择邮箱和 HTML 地址、默认 TXT，也可组合选择邮箱、API、HTML 地址并选择 TXT、CSV 或 JSON 文件格式；“复制选中/复制全部”会按当前字段和格式直接写入剪贴板。TXT 不带表头，每行保存一条邮箱记录，所选字段按邮箱、API、HTML 地址顺序使用 `----` 连接；CSV 可直接用 Excel 打开，JSON 便于程序读取。

“邮件”列显示当前邮箱的邮件数量，点击后弹出该邮箱专属邮件列表，可查询主题、发件人、收件时间、正文、验证码和隔离后的 HTML 邮件预览，并可直接同步或刷新当前邮箱邮件。系统默认只保存验证码邮件；管理员在“系统设置”关闭“只保留验证码邮件”后，IMAP 与旧接口同步会保存隐藏邮箱收到的全部邮件，但“最新验证码”和取码 API 仍只提取验证码。公开 HTML 接码页默认展示最近 50 封，管理员可在系统设置中调整为 1-500 封；最新验证码的查找不受展示数量限制。

全站品牌图标使用项目 Logo 图片，`/favicon.ico` 与页面 Logo 均由同一图片资源生成并随 Go 程序内嵌发布。

“全部邮箱”会保留并展示创建邮箱时填写的标签。HTML 地址未访问时显示“未激活”和激活后的有效期；首次访问后切换为“已激活”，并按天、时、分、秒显示剩余时间，悬停可查看准确激活与到期时间。管理员可在系统设置中直接输入有效秒数，界面实时预览对应的天、时、分、秒。

单邮箱操作菜单会跟随 API 状态切换：API 正常时显示“停用 API”，停用后显示“启用 API”；点击重新启用后，邮箱恢复可用并重新显示“停用 API”。

项目中的更新、停用、邮件清理、永久删除和账号删除询问框均由矩龙邮箱界面自身渲染，颜色会跟随当前主题变化；桌面端居中显示，窄屏设备使用底部弹层，不依赖浏览器原生确认框。

删除按钮会先查询 iCloud Hide My Email 地址：活跃地址先停用，再永久删除；远端成功或确认已不存在后才清理本地邮箱、邮件、API token 和 HTML 地址。远端失败时保留本地数据。该操作依赖邮箱所属 Apple 账号已保存旧接口 iCloud 登录态。

公开 HTML 接码页会识别 HTML 邮件并以隔离沙箱还原邮件排版，纯文本邮件继续按文本展示；脚本、表单、页面跳转和外部资源默认被拦截。页面右上角按天、时、分、秒显示链接剩余时间并每秒更新。邮件数据默认每 20 秒刷新，管理员可在系统设置中配置 5-3600 秒的刷新间隔；页面每次收到数据后会自动采用最新设置并重新校准倒计时。

邮箱入库时会自动生成 HTML 接码地址。新地址保持“待首次使用”，第一次打开页面或请求 `/data` 时才按系统设置开始计算有效期，重复访问不续期。地址到期后保留“已过期”状态，便于在“取件码与发货”按全部、已激活、未激活、已过期筛选；也可使用“清理全部已过期”逐个永久删除已出库邮箱。管理员开启“HTML 接码地址过期后自动删除邮箱”后，后台每分钟检查到期地址，先永久删除 iCloud 隐私邮箱，成功后再清理本地邮箱、邮件、API token 和 HTML 地址，失败时保留数据并重试。邮箱 API 始终使用独立 token，不受 HTML 地址过期时间影响。

## 2. 目录说明

- `cmd/panel/main.go`：程序入口。
- `internal/app/`：后端、Apple/iCloud 协议、数据存储、页面模板。
- `internal/app/templates/`：前端页面模板。
- `config.example.json`：示例配置。
- `README.md`：完整功能说明。
- `矩龙邮箱接入设计.md`：接口和设计记录。

源码包不包含真实数据：

- 不包含 `data/state.json`
- 不包含真实 `config.json`
- 不包含账号、Cookie、邮箱 API Key
- 不包含 `.git` 历史目录
- 不包含编译产物

## 3. Windows 本地运行

安装 Go 后，在项目目录执行：

```powershell
copy config.example.json config.json
go run ./cmd/panel --config config.json
```

默认打开：

```text
http://127.0.0.1:8787/
```

也可以编译成 exe：

```powershell
go build -trimpath -o julong-ic-email.exe ./cmd/panel
.\julong-ic-email.exe --config config.json
```

## 4. Linux 本地运行

```bash
cp config.example.json config.json
go run ./cmd/panel --config config.json
```

编译 Linux amd64：

```bash
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -ldflags="-s -w" -o julong-ic-email ./cmd/panel
chmod +x julong-ic-email
./julong-ic-email --config config.json
```

## 5. 第一次管理员账号

系统没有默认管理员账号密码。

根域名对未登录访客只显示矩龙邮箱 Logo、名称和欢迎语，不会重定向或展示后台入口。第一次打开后台登录/注册入口后，自己注册账号；首次部署默认入口为 `/manage`，管理员之后可在系统设置修改为自己的唯一路径：

- 第一个注册成功的账号会自动成为管理员。
- 后续注册账号是普通用户。

## 6. 配置文件

复制 `config.example.json` 为 `config.json` 后按需修改：

```json
{
  "host": "127.0.0.1",
  "port": 8787,
  "data_path": "data/state.json",
  "api_key": "",
  "public_base_url": "",
  "icloud_default_host": "www.icloud.com.cn",
  "icloud_client_id": "d39ba9916b7251055b22c7f910e2ea796ee65e98b2ddecea8f5dde8d9d1a815d"
}
```

关键字段：

- `host`：监听地址。本地建议 `127.0.0.1`。
- `port`：监听端口，默认 `8787`。
- `data_path`：数据文件路径。真实账号、登录态、邮箱和邮件缓存都在这里。
- `api_key`：全局自动取号 API Key，可留空。
- `public_base_url`：复制邮箱 API 时使用的外部域名，例如 `https://www.example.com`。
- `icloud_default_host`：Apple/iCloud 默认域，国内账号常用 `www.icloud.com.cn`，海外账号可用 `www.icloud.com`。

## 7. 服务器部署示例

推荐目录：

```text
/opt/julong-ic-email/
  julong-ic-email
  config.json
  data/state.json
```

创建目录：

```bash
mkdir -p /opt/julong-ic-email/data
cp julong-ic-email /opt/julong-ic-email/
cp config.example.json /opt/julong-ic-email/config.json
chmod +x /opt/julong-ic-email/julong-ic-email
```

编辑 `/opt/julong-ic-email/config.json`：

```json
{
  "host": "127.0.0.1",
  "port": 8787,
  "data_path": "/opt/julong-ic-email/data/state.json",
  "api_key": "",
  "public_base_url": "https://你的域名",
  "icloud_default_host": "www.icloud.com.cn",
  "icloud_client_id": "d39ba9916b7251055b22c7f910e2ea796ee65e98b2ddecea8f5dde8d9d1a815d"
}
```

## 8. systemd 示例

创建 `/etc/systemd/system/julong-ic-email.service`：

```ini
[Unit]
Description=矩龙邮箱
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/julong-ic-email
ExecStart=/opt/julong-ic-email/julong-ic-email --config /opt/julong-ic-email/config.json
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

启动：

```bash
systemctl daemon-reload
systemctl enable --now julong-ic-email
systemctl status julong-ic-email
```

查看日志：

```bash
journalctl -u julong-ic-email -f
```

## 9. Nginx 反代示例

```nginx
server {
    listen 80;
    server_name 你的域名;

    location / {
        proxy_pass http://127.0.0.1:8787;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

如果使用 HTTPS，请在 Nginx 或 Caddy 层配置证书。

## 10. 升级注意事项

升级程序时只替换二进制，不要覆盖数据文件：

```bash
systemctl stop julong-ic-email
cp julong-ic-email /opt/julong-ic-email/julong-ic-email
systemctl start julong-ic-email
```

升级前建议备份：

```bash
cp /opt/julong-ic-email/data/state.json /opt/julong-ic-email/data/state.json.bak
```

不要把自己的 `state.json` 发给别人。

## 11. 公开版发布说明

> 这是公开仓库版本，只保留通用部署方法和功能变更记录。真实线上域名、服务器 IP、SSH 端口、主机名、备份路径、账号数据、Cookie、API Key 和 `state.json` 均不应写入公开仓库。

### 公开版安全边界

可以公开：

- 源码、测试、模板页面和示例配置。
- `config.example.json`。
- README、设计说明和通用部署示例。

不要公开：

- 真实 `config.json`。
- `data/state.json` 或任何导出的状态文件。
- Apple/iCloud Cookie、`scnt`、session、验证码、密码、API Key。
- 真实服务器 IP、SSH 端口、用户名、主机名、Nginx 站点配置路径、线上备份路径。

### 公开版主要变更记录

- 新增域名邮箱产品线：矩龙邮箱现有工作台可管理 iCloud 隐私邮箱与自有域名邮箱，统一查看邮件、HTML 接码地址与单邮箱取码 API。
- 新增内置 receive-only SMTP 服务、域名资产管理、批量地址生成、精确收件地址校验与 DNS 指引；Docker 镜像公开 `2525` 端口，生产可将宿主机 TCP 25 映射到容器端口。
- 域名邮件能力采用 Go 重实现，参考 CloakMail（MIT）的域名收件与生命周期思路；保留矩龙邮箱既有 UI，未引入 CloakMail UI。第三方声明见 `THIRD_PARTY_NOTICES.md`。

- 首页和管理页改为左侧分类视图，只显示当前分类内容。
- 隐私邮箱数据页支持分页，默认每页 `10` 个，可自定义每页数量。
- 复制邮箱 API 的 TXT 格式为 `邮箱----API`。
- 管理员可以删除普通用户账号，并清理该账号关联的登录态、隐私邮箱和验证码邮件。
- 新增 Apple Account 新接口登录态，旧 iCloud Web 登录态和新接口登录态分别保存。
- 创建隐私邮箱时优先使用新接口；没有新接口登录态时回退旧 iCloud Hide My Email 接口。
- 新接口每小时约可创建 `20` 个，旧接口每小时约可创建 `5` 个；两种登录态都保存后可合计约 `25` 个/小时。
- 手动创建不再设置总数；当前勾选账号会在同一次请求里同时创建 1 轮。
- 定时创建只保留间隔配置，默认 `60` 分钟。
- 定时创建会按账号记录本轮可用接口：新接口失败后，本轮切到旧接口继续；同一账号新旧接口都失败后才临时跳过该账号，其他账号继续并发创建。
- 创建结果页面会显示成功邮箱和失败账号/失败原因。
- Apple Account 调试日志已脱敏，只输出请求摘要和指纹，不输出完整 Cookie、`scnt`、session、API key、账号邮箱、密码或验证码。

### 发布前检查

```powershell
go test -count=1 ./...
git status --short
```

公开发布前建议额外检查：

```powershell
rg -n "真实域名|真实服务器IP|password|cookie|token|api_key|state.json|config.json" .
```

命中示例配置、测试假数据、占位符和文档提醒是正常的；真实凭证、真实服务器地址和真实状态文件不能提交。

### 日常检查命令

```bash
systemctl status julong-ic-email.service --no-pager
journalctl -u julong-ic-email.service -n 80 --no-pager
curl -fsS http://127.0.0.1:8787/ >/dev/null
```

公网烟测请替换为自己的域名：

```bash
curl -I https://example.com/
curl -I https://example.com/你的后台入口
```

### 标准升级方式

本项目模板通过 `go:embed` 打进二进制，升级前端和后端都只需要替换 Linux 二进制。

本地构建：

```powershell
$env:CGO_ENABLED='0'
$env:GOOS='linux'
$env:GOARCH='amd64'
go build -trimpath -ldflags "-s -w" -o julong-ic-email ./cmd/panel
```

远端升级原则：

1. 新建 `/opt/julong-ic-email/releases/<release-name>/`。
2. 上传新二进制到该 release 目录并设置 `0755`。
3. 写入 `julong-ic-email.sha256` 并执行 `sha256sum -c`。
4. 记录旧 `current` 指向到 `/opt/julong-ic-email/backups/<release-name>-previous-release.txt`。
5. 原子切换 `/opt/julong-ic-email/current` 软链接。
6. `systemctl restart julong-ic-email.service`。
7. 先测 `http://127.0.0.1:8787/`，再测试公网域名下配置的后台入口。

不要上传或覆盖：

- `/opt/julong-ic-email/shared/config.json`
- `/opt/julong-ic-email/shared/data/state.json`
- `/opt/julong-ic-email/shared/` 下任何真实运行数据

### 回滚方式

如果新版本服务启动失败或烟测失败，把 `current` 切回上一个 release 后重启：

```bash
old=/opt/julong-ic-email/releases/<previous-release-name>
ln -sfn "$old" /opt/julong-ic-email/current.next
mv -Tf /opt/julong-ic-email/current.next /opt/julong-ic-email/current
systemctl restart julong-ic-email.service
systemctl status julong-ic-email.service --no-pager
```

如果是后续版本，优先读取对应备份记录：

```bash
cat /opt/julong-ic-email/backups/<release-name>-previous-release.txt
```

## 12. 常用操作

启动后进入首页：

```text
http://127.0.0.1:8787/
```

使用流程：

1. 注册第一个管理员账号。
2. 登录后台。
3. 在页面里保存 Apple/iCloud 登录态。
4. 创建或同步 Hide My Email 隐私邮箱。
5. 复制每个邮箱的 API 地址给其他项目取验证码。

对外取码 API 格式：

```text
http://你的域名/api/v1/mailboxes/邮箱地址/code?key=邮箱独立Key
```

如果短时间重新发送验证码，普通取码接口会跳过已经返回过的同一封旧验证码邮件；需要排查缓存时才加：

```text
&cache=1
```
